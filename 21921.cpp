#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, x;
    vector<int> arr;
    for (int i = 0; i < n; i++)
    {
        int num;
        cin >> num;

        arr.push_back(num);
    }
    int cnt = 0;
}