#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int check1[26];
int check2[26];

int main()
{
    int num, cnt = 0;
    cin >> num;
    vector<string> s;

    for (int i = 0; i < num; i++)
    {
        string str;
        cin >> str;

        s.push_back(str);
    }

    for (int i = 0; i < num; i++)
    {
        for (int j = 0; j < s[num].length(); j++)
        {
            c
        }
    }
    return 0;
}